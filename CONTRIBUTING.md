# 貢獻指南

本項目的價值取決於條文的準確性。以下規則圍繞這一點。

## 基本原則

1. **每條控制點必須可追溯到官方原文。** `sourceId` 指向 `data/sources.js` 中一份有官方連結的文件，
   `clause` 填寫條文中的實際條款編號（如 `1.1`、`(B)(iii)`、`7.3.4`）。不接受「綜合業界實踐」類條目。
2. **英文來源文字必須正確分類。** `quoteStatus` 使用 `verbatim`（逐字原文）、`excerpt`（清楚標示省略的節錄）
   或 `summary`（標題／英文來源說明）。`verbatim` 不得改寫或合併多段。中文說明寫在 `requirement`；
   如中文說明與監管機構原文有出入，以原文為準。
3. **不要憑記憶填日期。** 發布日期以監管機構官網顯示為準。修改 `sources.js` 時請一併更新
   `data/_registry.js` 中的 `meta.verifiedOn`。
4. **法律地位要寫清楚。** `legalStatus` 需區分法定指引、非法定指引、通函、實務守則、法例——
   合規後果不同，不能混為一談。

## 提交前必做

```bash
node tools/validate.mjs
node --test tests/*.test.mjs
```

校驗項：ID 唯一、出處存在、控制域／牌照／業務特徵有效、交叉引用可解析、必填欄位齊全、日期格式正確。
校驗不通過的 PR 不會合並。

改動界面邏輯時請在瀏覽器中實測（雙擊 `index.html` 即可，不需要伺服器）：
至少驗證「選擇牌照 → 出現控制點 → 自評 → 匯出 CSV」這條主路徑。

## 關於交叉引用（`crossRefs`）

`crossRefs` 有兩種用途，取決於是否雙向：

- **雙向**（A 引用 B 且 B 引用 A）→ 界面會**合併**為一張卡片，視為同一項要求
- **單向** → 只顯示為「另見」標籤，不合並

**只在兩條條文確實要求同一件事時才建立雙向引用。** 若一方範圍明顯更寬（例如 TM-G-1 第 3 節「保安管理」
之於 PDPO DPP4），請用單向引用。錯誤的雙向引用會讓使用者誤以為滿足一方即滿足另一方。

## 新增一份法規

1. 在 `data/sources.js` 加入出處條目（含官方連結、發布日期、法律地位）
2. 在 `data/controls/` 新建或擴充對應文件
3. 在 `index.html` 的 `<script>` 列表中加入新文件
4. 更新 `README.md` 的覆蓋範圍表格與徽章中的控制點數量
5. 執行校驗並在瀏覽器實測

## 什麼不適合提交

- 沒有官方出處的「最佳實踐」建議
- 對條文的個人解讀或合規意見（本工具刻意不提供意見）
- 需要建置步驟或引入執行時依賴的改動——零依賴、雙擊可用是本項目的硬約束
