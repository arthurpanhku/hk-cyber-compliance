# 香港网络安全合规助手：实用化开发计划

> 状态：已根据 Claude Review 修订；P0／P1 已于 `codex/practical-assessment-workflow` 分支完成开发及测试
>
> 修订日期：2026-09-09
>
> 产品目标：用最少功能，让香港金融业网络安全、科技风险、合规及内审人员能够完成一次真实的合规差距评估，并形成可以持续跟进和交接的工作底稿。

## 1. 产品定位

本项目不是重型 GRC 平台，也不代替法律或合规判断。它只完成一个清晰闭环：

1. 判断哪些监管要求适用；
2. 记录每项要求的实施状态和判断依据；
3. 找出需要评估或整改的事项；
4. 导出可供内部讨论、审计或顾问复核的底稿；
5. 下次打开或换一台电脑后可以继续工作。

核心用户：

- SFC 持牌法团、虚拟资产交易平台、认可机构及储值支付工具持牌人的网络安全／科技风险人员；
- 金融机构合规及内部审计人员；
- 为上述机构提供服务的网络安全顾问。

暂不纳入 SOC 日常运营、漏洞扫描、渗透测试或事件工单场景。

## 2. 必须保持的产品约束

- 零运行时依赖、无需构建；
- 双击 `index.html` 后可通过 `file://` 完整运行；
- 不建立账号、后端或云端数据库；
- 评估资料默认只存于用户浏览器和用户主动导出的项目文件；
- 不上传证据文件，不声称自动判断机构是否合规；
- 每个开发阶段结束时，现有应用必须仍可独立运行。

## 3. 要解决的五个真实痛点

### 痛点 A：不知道哪些要求适用于自己

用户不仅要看到控制清单，还需要知道“为什么适用”。

最小解决方案：

- 保留现有牌照及业务特征筛选；
- 控制卡显示触发它的监管来源、已选择牌照和业务特征；
- 只对少数有额外条件的条文增加 `applicabilityNote`，例如第 9 类的特定互联网交易适用范围；
- 不建设规则编辑器，也不为全部 102 条重复撰写适用性说明。

### 痛点 B：勾选后没有足够记录支持复核

“已实施／部分／未实施／不适用”不足以说明判断依据。

每个监管控制增加四个可选字段：

- 实施说明；
- 证据引用：文件名、内部链接、工单号或系统名称，只保存文字；
- 负责人；
- 目标完成日期。

选择“不适用”时，界面将“实施说明”提示文字改为“不适用理由”，但首版不强制填写，避免阻塞快速评估。

### 痛点 C：缺口没有形成可执行清单

整改清单应包括：

- 未评；
- 部分实施；
- 未实施。

首版只支持按目标日期排序、按控制域筛选，并标出已逾期和 30 日内到期项目。逾期以项目的评估基准日期 `asOfDate` 计算，使历史报告可复现。

负责人筛选先不开发；用户可在 CSV 中筛选。

### 痛点 D：结果被困在一个浏览器里

`localStorage` 继续用于自动保存，同时增加带版本号的 JSON 项目文件，用于备份和交接。

最小功能：

- 导出项目；
- 导入项目；
- 导入前显示项目名称、导出时间、控制数据版本和记录数量；
- 导入成功后立即写回 `localStorage`；
- 重置前提示用户先导出备份。

JSON 是备份和交接格式，不是多人同步机制。

### 痛点 E：数据更新后不知道哪些结论仍有效

首两个阶段只突出显示条文核验日期和控制数据版本。旧项目导入时报告版本差异，但不开发自动法规监控。

新增、修改、删除控制的重评流程属于 P2，必须等真实用户验证后再决定是否开发。

## 4. 合并控制项的确定设计

这是开发前必须固定的设计，不再留给实现阶段临时决定。

### 4.1 保存原则

- 状态和四个工作记录字段始终以单个控制 ID 保存；
- 动态合并组没有独立状态，也不能成为存储键；
- 合并／取消合并不能改变任何控制的底层记录。

### 4.2 合并卡交互

- 合并卡保留一个共同标题和跨监管说明；
- 每个监管来源显示一行紧凑的状态选择器；
- 点击某一来源的“记录”后，展开该控制的四个工作字段；
- 卡片顶部只显示“全部完成”或“仍有 N 项待处理”的摘要，不计算或保存组状态；
- “待处理”是状态为未评、部分或未实施的成员；“已实施”和“不适用”不计入待处理；
- 不提供“一键把整组合规”按钮，避免误把范围相近但不完全相同的条文同时判定为完成。

### 4.3 v1 本地数据迁移

现有 v1 以合并组首个控制 ID 保存状态。首次加载 v2 时：

1. 如找到有效 v2 状态，直接使用，不重复迁移；
2. 如只有 `hkcc.state.v1`，读取其 `merge` 值；
3. 若 `merge === false`，旧状态只保留到原控制 ID；
4. 若 `merge === true`，按保存的牌照、业务特征和当前双向交叉引用重建当时可见合并组，将组首状态传播到组内全部控制；
5. 无法识别的控制 ID 保存到 `unresolvedAssessments`，不得静默删除；
6. 迁移成功后写入 v2，但至少保留 v1 原数据一个版本，供手动恢复；
7. 迁移失败时继续使用 v1 数据并提示导出，不覆盖原数据。

## 5. 引文与来源可信度设计

当前部分 `quote` 实际是标题或摘要。P0 必须完成数据分类，避免把说明展示成逐字原文。

每个英文来源文本增加：

```js
{
  quote: "...",
  quoteStatus: "verbatim" // verbatim | excerpt | summary
}
```

界面分别显示：

- `verbatim`：英文条文原文；
- `excerpt`：英文条文节录；
- `summary`：英文来源说明。

校验规则：

- 有 `quote` 必须有合法的 `quoteStatus`；
- `verbatim` 和 `excerpt` 必须有明确 `clause`；
- 省略原文的 `excerpt` 必须清楚保留省略号；
- 无法核实的文字不得标为 `verbatim`；
- 来源继续保留官方链接、发布日期、法律地位和全局核验日期；只补齐现有缺失值，不另建复杂版本库。

## 6. P0：可信基础

目标：先消除误导、数据转移和不可测试问题，再增加功能。

任务：

1. 将“自评合规度”改为“控制实施完成度”，注明部分实施按 50% 计算仅用于进度显示，不代表监管结论；
2. 把适用性和双向合并等纯逻辑抽取到 `js/engine.js`；
3. 实施第 4 节的逐控制 ID 状态和 v1 迁移设计；
4. 实施 `quoteStatus`，逐条核对所有现有引文并扩展校验器；
5. 补齐少数来源缺失的发布日期或版本信息；
6. 将界面及数据文案统一为香港常用繁体中文，并调整繁体中文字体栈；不建设 i18n；
7. 使用 Node 内置 `node --test` 建立零依赖测试。

验收标准：

- `node tools/validate.mjs` 通过；
- `node --test tests/*.test.mjs` 通过；
- 合并开关切换前后，逐控制 ID 状态快照完全一致；
- v1 合并状态按规则传播，迁移失败不会覆盖原数据；
- 所有显示为“英文条文原文”的文字均已人工核实；
- 页面不再把产品分数表达为法律上的合规结论；
- 双击 `index.html` 后主路径仍可离线运行。

## 7. P1：真实评估最小闭环

目标：用户可以完成、保存、恢复和交付一次真实评估。

任务：

1. 增加项目名称和评估基准日期；
2. 为每个控制 ID 增加四个工作记录字段；
3. 在合并卡按监管来源编辑状态和工作记录；
4. 显示业务特征标签及少量 `applicabilityNote`；
5. 增加包含未评、部分、未实施的整改清单；
6. 按基准日期显示逾期和 30 日内到期；
7. 扩展 CSV 和打印版；
8. 增加安全的 JSON 项目导入／导出；
9. 重置前提示导出项目备份；
10. 为新功能补充迁移、往返、损坏输入和 CSV 注入测试。

验收标准：

- CSV 可以回答：要求是什么、为什么适用、当前状态、判断依据、证据在哪里、谁负责、何时完成；
- JSON 导出、导入、再次导出后业务字段一致；时间戳等导出元数据可以不同；
- 导入成功后内存状态和 `localStorage` 一致；
- 合并视图不会隐藏任何来源的状态或工作记录；
- 整改清单不会在全部未评时错误显示为空；
- CSV 中潜在公式内容已安全中和；
- `node tools/validate.mjs`、`node --test tests/*.test.mjs` 和离线人工冒烟全部通过。

## 8. JSON 项目格式与导入规则

建议格式：

```js
{
  schemaVersion: 2,
  controlDataVersion: "1.2.0",
  exportedAt: "2026-09-09T08:00:00.000Z",
  project: {
    name: "示例机构 2026 年网络安全评估",
    asOfDate: "2026-09-09"
  },
  scope: {
    licenses: ["sfc-ra1"],
    attributes: ["internet-trading", "personal-data"]
  },
  assessments: {
    "SFC-IT-1.1": {
      status: "partial",
      implementationNote: "网页端已实施，流动应用程式正在改造",
      evidenceRef: "IAM Standard v3; SEC-241",
      owner: "Technology Risk",
      targetDate: "2026-12-31",
      assessedAt: "2026-09-09"
    }
  },
  unresolvedAssessments: {}
}
```

其中 `controlDataVersion` 必须直接取自 `HKCC.meta.version`，不得在导出逻辑中另行维护。

导入规则：

- 文件必须是 JSON 对象且不超过预设大小；首版上限为 5 MB；
- `schemaVersion` 高于当前支持版本时拒绝导入并提示升级应用；
- 低版本必须先通过显式迁移函数，不能直接合并对象；
- 未知控制 ID 移入 `unresolvedAssessments` 并在确认界面报告数量；
- 未知牌照和业务特征丢弃并报告；
- 核心结构、状态、字段类型或日期不合法时拒绝整个文件并列出错误，不进行部分导入；
- 项目名称和负责人最长 200 字符，实施说明和证据引用各最长 10,000 字符；超限视为无效文件；
- 导入前只显示摘要，不改变当前状态；
- 用户确认后才替换当前项目，并立即写入 `localStorage`；
- 任一步骤失败时保留导入前状态；
- 自由文本在页面继续通过 `textContent` 显示，不渲染不可信 HTML。

## 9. CSV 和前端安全规则

- CSV 保留 UTF-8 BOM 和双引号转义；
- 对以 `=`, `+`, `-`, `@`、制表符或回车开头的用户输入增加单引号前缀，防止电子表格公式注入；
- CSV 和打印版逐控制 ID 输出，不以合并卡作为数据行；
- JSON 文件不得包含浏览器本地路径、账号凭证或证据附件；
- 导入文件中的字符串必须限制合理长度，避免异常文件令页面失去响应；
- 不使用 `innerHTML` 渲染项目文件中的自由文本。

## 10. P2：版本变化，真实用户验证后再决定

候选任务：

1. 为控制增加 `introducedIn` 和可选的 `updatedIn`；
2. 导入旧项目时标记新增、已更新和已删除控制；
3. 支持筛选“需要重新评估”；
4. CSV 加入重新评估标记；
5. 使用 `data/changelog.js` 注册结构化变化摘要。

不得通过运行时 `fetch()` 读取 `CHANGELOG.md`，因为这会破坏 `file://` 离线运行。

## 11. 明确不做的功能

P0 和 P1 不开发：

- 用户注册、登录、角色权限、云端同步；
- 后端数据库及 SaaS 托管；
- 证据文件上传；
- 多人实时协作、评论或审批流；
- AI 法律解释或自动判断合规；
- 漏洞扫描、资产发现、SIEM 或事件响应系统；
- 自定义法规／控制框架编辑器；
- 负责人筛选和复杂管理驾驶舱；
- 自动法规爬虫、邮件提醒；
- 多项目管理、英文界面、Excel 原生格式或外部工单集成。

后五项只有在真实用户证明当前最小工作流不够用时才重新评估。

## 12. 测试计划

### 数据校验

1. ID 唯一，出处、控制域、牌照和业务特征存在；
2. 交叉引用可解析；
3. `quoteStatus` 合法，原文或节录具有条款位置；
4. 日期和状态枚举合法；
5. 页面运行时脚本不依赖 `fetch()`、`XMLHttpRequest` 或 ES module 读取本地数据。

### Node 单元测试

6. 多牌照任一命中，多业务特征全部命中，未选牌照不显示控制；
7. 双向引用才合并，单向引用不合并，结果顺序确定；
8. 合并开关切换后逐控制状态不变；
9. v1 `merge: true` 正确传播，`merge: false` 保留原 ID，失败不覆盖原状态；
10. JSON 往返业务字段一致；
11. 未来 schema、损坏 JSON、超大文件、未知 ID、非法状态和非法日期均按规则处理；
12. 导入成功后 `localStorage` 与内存一致；
13. CSV 正确处理逗号、引号、换行、中英文和公式前缀，并保留 BOM。

### 人工端到端冒烟

14. `file://` 双击运行完整主路径；
15. GitHub Pages 部署可用，保留 `.nojekyll`；
16. 移动端、桌面端及打印／PDF 中新字段可用；
17. 禁用 `localStorage` 后应用仍可运行，并提示用户导出项目；
18. 导入确认显示项目资料、版本差异、有效及未识别记录数量；
19. 重置前提示备份，不确认则不改变状态。

## 13. 开发拆分

### PR 1 — 状态模型与测试基础

- 抽取 `js/engine.js`；
- 改为逐控制 ID 状态；
- 实施 v1 迁移；
- 修复合并显示及完成度命名；
- 增加状态、适用性和合并测试。

### PR 2 — 数据可信度与香港化

- 增加并校验 `quoteStatus`；
- 完成人工引文核对；
- 补齐来源元数据；
- 统一香港繁体中文和字体；
- 更新 README 和贡献规则。

### PR 3 — 工作记录

- 增加项目资料及四个记录字段；
- 实施合并卡的按来源编辑；
- 扩展 v2 本地状态以保存项目资料和工作记录；
- 扩展本地保存和打印基础信息。

### PR 4 — 适用原因与整改清单

- 显示业务特征和 `applicabilityNote`；
- 增加未评／部分／未实施清单；
- 增加目标日期排序、控制域筛选和到期提示。

### PR 5 — 可交付与可携带

- 扩展 CSV 和打印版；
- 增加 CSV 公式注入防护；
- 实施 JSON 导入／导出、确认、异常报告和安全限制；
- 完成往返、损坏输入和人工冒烟测试。

每个 PR 必须独立可运行和回归。P1 完成后暂停功能开发，先做真实用户验证。

## 14. 真实用户验证

邀请 5–10 名目标用户用真实或脱敏资料完成一次评估，观察：

- 是否能在两分钟内正确选择范围；
- 是否仍要回到 Excel 或 Word 才能完成记录；
- 四个工作字段哪些经常使用，哪些没有价值；
- 导出结果是否足够让主管、内审或外部顾问复核；
- 一周后能否顺利继续上次工作；
- 是否有人把“控制实施完成度”误解为监管合规结论；
- 合并卡按监管来源编辑是否清楚，还是过于复杂。

继续开发 P2 或其他功能的最低条件：至少 3 名用户愿意在下一次评估中再次使用，而不只是认为产品“看起来不错”。

## 15. Claude Review 处理记录

本次修订已处理 `CLAUDE_REVIEW.md` 的 B1–B8：

- B1/B5：明确逐控制 ID 存储、合并卡交互和 v1 迁移；
- B2：增加 `quoteStatus` 及人工／自动校验；
- B3：加入 CSV 公式注入防护；
- B4：定义 JSON 全部分支、确认和回滚规则；
- B6：改用 `data/changelog.js`，禁止读取本地 Markdown；
- B7：整改清单加入未评，并以 `asOfDate` 判断逾期；
- B8：明确抽取 `js/engine.js` 并使用 Node 内置测试。

同时采纳了简化建议：不做负责人筛选，不为全部控制重复撰写适用原因，不新增来源版本库，并把不可自动验证的指标移入真实用户验证。
