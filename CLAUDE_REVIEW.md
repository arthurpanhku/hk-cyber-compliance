# Claude 对实用化开发计划的评审

> 评审对象：`DEVELOPMENT_PLAN.md`
>
> 评审方式：Claude Code 2.1.197，只读模式；结合仓库 v1.1.0 实现检查；未修改任何代码或文件。
>
> 总体结论：计划方向正确、克制力强，但合并状态修复方案不完整；CSV 注入和 JSON 导入行为尚未定义；从 `CHANGELOG.md` 读取变化会破坏 `file://` 离线约束；另有 27 条以上“英文原文”实际为标题或摘要。

## 1. 必须在开发前解决的问题

### B1. 合并状态修复方案不完整

`js/app.js` 以合并组首个成员 ID 保存整组状态。组内排序可能令 HKMA 控制成为组首；用户在合并视图选择状态后，取消合并会令其他 SFC 控制显示为未评，摘要百分比也会变化。

开发前必须决定：

- 合并卡是否为每个成员显示独立评估组件，或采用聚合状态；
- 如采用聚合状态，聚合规则是否取最差状态；
- v1 `localStorage` 中以组首 ID 保存的状态如何无损迁移到逐控制 ID；
- “只看未完成”必须按成员状态判断，而不是继续依赖动态组首 ID。

Claude 建议：所有状态及工作记录均按单个控制 ID 存储；合并卡为每个成员提供独立评估资料。迁移 v1 状态时，按当前双向交叉引用计算合并组，把旧组状态传播到全部成员。

### B2. “英文条文原文”的数据可信度问题比计划预期更大

当前渲染器把所有非空 `quote` 显示为“英文条文原文”，校验器只检查字段是否存在。至少 27 条内容实际是章节标题、原则名称或文件名称，另有若干省略号节录没有明确标注。

涉及位置包括：

- `data/controls/hkma.js` 中多项 TM-G-1、TM-E-1、OR-2、SA-2、C-RAF 控制；
- `data/controls/pdpo.js` 中七项 DPP 控制；
- `data/controls/svf.js` 中一项界别守则控制；
- `data/controls/sfc-internet-trading.js` 和 `data/controls/critical-infrastructure.js` 中若干节录。

建议：

- 增加 `quoteStatus: 'verbatim' | 'excerpt' | 'summary'`；
- 界面分别显示“英文原文／英文节录／英文说明”；
- 校验器强制每项 `quote` 声明状态；
- 标为 `verbatim` 的内容必须有可定位的条款编号。

### B3. CSV 公式注入防护缺失

当前 CSV 只转义双引号。P1 加入用户自由文本后，以 `=`, `+`, `-`, `@`、制表符或回车开头的内容可能被 Excel 当作公式执行。

建议在 CSV 单元格输出前检测 `/^[=+\-@\t\r]/`，命中时加单引号前缀，并增加对应测试。

### B4. JSON 导入行为没有完整定义

开发前应明确：

- 高于当前支持版本的 `schemaVersion`：拒绝导入并提示；
- 未知控制 ID：保留在隔离区，并在确认界面显示数量；
- 未知牌照或业务特征：丢弃并报告；
- 导入成功后立即写回 `localStorage`；
- 校验文件大小、对象类型、状态枚举和日期格式；
- v1 本地状态与 v2 项目文件使用同一条显式迁移路径。

### B5. 四个工作记录字段与合并视图的关系未定义

实施说明、证据引用、负责人和目标日期也必须以单个控制 ID 保存。如果合并卡只编辑组首成员，会重演状态丢失问题。CSV 和打印版也应逐控制成员输出。

### B6. 从 `CHANGELOG.md` 读取变化会破坏离线约束

浏览器通过 `file://` 打开时不能可靠 `fetch()` 本地 Markdown；这正是当前监管数据采用 `.js` 而不是 `.json` 的原因。

建议如未来实施 P2，使用 `data/changelog.js` 注册结构化变化摘要。`controlDataVersion` 必须直接取自 `HKCC.meta.version`，不能另行手填。

### B7. 整改清单不应排除“未评”

新用户刚选定范围时，所有项目都是未评；如果整改清单只显示“部分”和“未实施”，页面会是空的。应显示“未评、部分、未实施”三类，并清楚区分。

逾期判断建议以项目的评估基准日期 `asOfDate` 为准，使历史审计结果可复现。

### B8. 自动化测试需要先抽取纯逻辑

当前 `js/app.js` 是单个 IIFE，`applies()`、`cluster()` 等函数无法直接单测。建议：

- 抽取到普通脚本 `js/engine.js`，通过 `window` 暴露给页面；
- 测试使用 Node 内置 `node --test`；
- 不安装测试框架或新增构建步骤；
- `index.html` 在 `app.js` 前加载 `engine.js`。

## 2. 建议简化或推迟的内容

1. 来源元数据大部分已经存在。P0 只需补齐少数缺少发布日期的来源，不需要新建复杂结构。
2. 香港繁体中文采用一次性字词表替换，并调整字体栈；不建设国际化框架。
3. “适用原因”只需补充触发的业务特征标签，并对少数有条件条文增加 `applicabilityNote`；不必为全部 102 条新增说明。
4. 整改清单首版保留目标日期排序、逾期／30 日内到期分组和控制域筛选；负责人排序与筛选可推迟，CSV 已能满足外部分析。
5. P2 全部推迟到真实用户验证后；暂不开发变化面板。
6. “两分钟内选定范围”和“可直接交给主管复核”属于用户验证指标，不应作为自动化验收标准。
7. 项目 JSON 的 `exportedAt` 应放在顶层，而不是 `project` 对象中。

## 3. 建议保留的内容

- 产品定位及明确的“不做功能”清单；
- 四个工作记录字段，并坚持不做附件库、审批流和评论系统；
- `localStorage` 自动保存、JSON 文件用于备份及交接；
- 导入前显示项目名称、时间、数据版本和项目数量；
- `schemaVersion` 显式迁移、控制 ID 为键、使用 `textContent`、统一日期格式；
- P0 先于 P1、每个 PR 都保持应用可运行；
- 5–10 名真实用户试用及至少 3 人愿意再次使用的验证门槛；
- CSV、安全降级和 `localStorage` 不可用场景的测试。

## 4. Claude 建议的 P0／P1 范围

### P0：可信基础

1. 状态模型与合并一致性：逐控制 ID 存储、合并卡按成员显示、v1 迁移传播、未完成筛选按成员判断；
2. 将“合规度”改为“控制实施完成度”，注明 50% 仅为进度计算；
3. 引文可信度：`quoteStatus`、差异化渲染、校验器强制及人工核对；
4. 香港繁体字词和字体栈调整；
5. 只补齐缺失的来源发布日期；
6. 抽取 `js/engine.js` 并使用 `node --test` 建立零依赖测试。

### P1：真实评估最小闭环

1. 项目名称和评估基准日期，并带入打印、CSV 和 JSON；
2. 四个工作记录字段按控制 ID 保存，合并卡按成员编辑；
3. 标记不适用时提示填写理由，重置前提示导出备份；
4. 适用原因只增加业务特征标签和可选 `applicabilityNote`；
5. 整改清单包括未评、部分及未实施，按日期排序，以基准日期计算逾期；
6. 扩展 CSV／打印并加入公式注入防护；
7. JSON 导入导出覆盖全部异常分支并写回本地状态；
8. 增加迁移、往返和注入测试。

可自动验证的完成标准：

- `node tools/validate.mjs` 和 `node --test tests/*.test.mjs` 全部通过；
- JSON 导出、导入、再导出后逐字段一致；
- 合并开关切换前后，逐控制 ID 状态快照不变；
- v1 状态迁移后没有控制意外变成未评；
- CSV 包含全部新字段并中和公式注入；
- 双击 `index.html` 后离线主路径可用。

## 5. 可执行测试清单

### 数据完整性

1. 保留现有 ID、出处、控制域、牌照、属性及交叉引用校验；
2. `quote` 必须带 `quoteStatus`，`verbatim` 必须有明确条款位置；
3. 增加防回归检查，确保页面脚本不通过 `fetch()`、`XMLHttpRequest` 或 ES module import 读取本地数据。

### 纯逻辑单元测试

4. 多牌照任一命中、多属性全部命中、未选牌照不显示控制；
5. 只有双向引用才合并，单向引用不合并，排序结果确定；
6. 合并开关反复切换后，逐控制 ID 状态不变；
7. v1 组首状态正确传播，未知控制进入隔离区，损坏 JSON 被安全拒绝；
8. 导入导出逐字段往返一致；未来 schema、未知牌照、非法状态、非法日期及超大文件分别测试；
9. CSV 覆盖逗号、引号、换行、中英文和公式前缀，验证 BOM 及新列。

### 人工端到端冒烟

10. `file://` 双击及 GitHub Pages 两种方式运行；
11. 检查移动端和打印／PDF，新字段必须可见；
12. 禁用 `localStorage` 后应用仍可运行并提示备份；
13. 导入确认显示项目资料和计数，重置前提示导出。

## 6. 对原计划的最终建议

继续保持 P0 → P1 → 真实用户验证 → 再决定 P2 的顺序。开发前先把 B1–B8 写回计划，其中 B1、B3、B4 和 B5 属于数据丢失或安全风险，必须先确定设计；B2 属于产品可信度底线；B6 属于离线架构约束。
